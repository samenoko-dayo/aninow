// Update the grid and scroll to the top when the active day changes
function updateGrid(activeDay) {
    // Your existing logic to update the grid based on activeDay

    // Scroll to the top of the page
    window.scrollTo(0, 0);
}